# General
This is a tool designed to be run on startup in order to disable 
the annoying automatic focus property of Microsoft® LifeCam HD-5000 webcams.

# Building
In case of missing libs/dependencies, try running the following command:
```
dotnet nuget add source --name nuget.org https://api.nuget.org/v3/index.json
```